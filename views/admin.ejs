<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Adminbereich – Medizinisches Cannabis</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <header class="header">
    <div class="logo-container">
      <div class="top-row">
        <img src="/images/wanncannabis-logo-animated.svg" alt="WannCannaBis Logo" class="logo" />
        <span class="title">Medizinisches Cannabis – Admin</span>
      </div>
      <span class="subtitle">Ein Unternehmen von WannCannaBis</span>
    </div>
    <nav>
      <a href="/admin" class="nav-link">Dashboard</a>
      <a href="/admin/products" class="nav-link">Sorten verwalten</a>
      <a href="/admin/newsletter" class="nav-link">Newsletter</a>
      <a href="/admin/prescriptions" class="nav-link">Rezepte</a>
      <a href="/inbox" class="nav-link">Nachrichten<span id="unread-badge" class="badge"></span></a>
      <a href="/logout" class="nav-link">Logout</a>
    </nav>
  </header>
  <main class="container">
    <div class="card">
      <h2>Benutzerverwaltung</h2>
      <% if (error) { %>
        <p class="error-message"><%= error %></p>
      <% } %>
      <table class="user-table">
        <thead>
          <tr>
            <th>ID</th>
            <th>Benutzername</th>
            <th>E-Mail</th>
            <th>Telefon</th>
            <th>Newsletter</th>
            <th>Admin</th>
            <th>Freigegeben</th>
            <th>Aktion</th>
            <th>Nachricht</th>
          </tr>
        </thead>
        <tbody>
          <% users.forEach(function(u) { %>
            <tr>
              <td><%= u.id %></td>
              <td><%= u.username || '' %></td>
              <td><%= u.email %></td>
              <td><%= u.phone || '' %></td>
              <td><%= u.newsletter_opt_in ? 'Ja' : 'Nein' %></td>
              <td><%= u.is_admin ? 'Ja' : 'Nein' %></td>
              <td><%= u.approved ? 'Ja' : 'Nein' %></td>
              <td>
                <% if (!u.is_admin && !u.approved) { %>
                  <form method="post" action="/admin/approve/<%= u.id %>" style="display:inline-block;">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                    <button type="submit" class="btn small">Freigeben</button>
                  </form>
                <% } %>
              </td>
              <td>
                <% if (!u.is_admin) { %>
                  <a href="/admin/users/<%= u.id %>/message" class="btn small">Nachricht</a>
                <% } %>
              </td>
            </tr>
          <% }); %>
        </tbody>
      </table>
    </div>
    <div style="margin-top: 1rem;">
      <a href="/showroom" class="btn back-btn">Zurück</a>
      <a href="/showroom" class="btn home-btn">Home</a>
    </div>
  </main>

  <!-- Qualitätssiegel im unteren Bereich -->
  <footer class="footer">
    <img src="/images/HOOD339_Qualitaetssiegel_silber.svg" alt="Qualitätssiegel" class="seal" />
  </footer>

  <!-- Unread message polling script -->
  <script src="/js/unread.js"></script>
</body>
</html>